import React from "react";
import ReactDOM from "react-dom/client";
import App from "./components/app"; // Certifique-se de que o caminho para o App.js esteja correto

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />); // use apenas o 'root.render'
